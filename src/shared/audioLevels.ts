/** Cycling volume presets for the commentary track (index 0 = mute). */
export const COMMENTARY_LEVELS = [0, 0.4, 0.7, 1.0] as const;

/** Cycling volume presets for the background music track (index 0 = mute). */
export const BG_LEVELS = [0, 0.1, 0.18, 0.35] as const;
